{
  "name": "portfolio-jest-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "node_api"
  ],
  "scripts": {
    "build": "npm run build --workspace portfolio-frontend",
    "test": "jest --config jest.config.js --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
